version: '3.8'

services:
  pixel-art-generator:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "80:80"
    restart: unless-stopped
    container_name: pixel-art-generator